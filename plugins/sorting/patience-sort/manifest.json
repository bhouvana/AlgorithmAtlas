{
  "schema_version": "1.0",
  "id": "patience-sort",
  "name": "Patience Sort",
  "version": "1.0.0",
  "category": "sorting",
  "visualization_type": "ARRAY_BARS",
  "entry_point": "algorithm.py",
  "benchmark_enabled": true,
  "description": "Sort by dealing cards into piles (like the card game) then merging with a min-heap.",
  "intuition": "Place each card on the leftmost pile whose top card is ≥ it. The number of piles equals the LIS length. Merge piles with a heap to produce sorted output in O(n log n).",
  "complexity": {
    "time_best": "O(n log n)",
    "time_average": "O(n log n)",
    "time_worst": "O(n log n)",
    "space": "O(n)"
  },
  "parameters": {
    "array_size": {
      "type": "integer",
      "label": "Array size",
      "min": 6,
      "max": 20,
      "default": 12
    }
  },
  "tags": ["sorting", "patience", "lis", "heap", "merge"],
  "references": []
}
