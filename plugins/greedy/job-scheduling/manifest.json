{
  "schema_version": "1.0",
  "id": "job-scheduling",
  "name": "Job Scheduling (Deadline)",
  "version": "1.0.0",
  "category": "greedy",
  "visualization_type": "ARRAY_BARS",
  "entry_point": "algorithm.py",
  "benchmark_enabled": true,
  "description": "Schedule jobs with deadlines and profits to maximize total profit, fitting each job in the latest available slot before its deadline.",
  "intuition": "Sort jobs by profit descending. Greedily assign each job to the latest available time slot before its deadline. This maximizes profit since we always try the highest-profit jobs first.",
  "complexity": {
    "time_best": "O(n²)",
    "time_average": "O(n²)",
    "time_worst": "O(n²)",
    "space": "O(n)"
  },
  "parameters": {
    "array_size": {
      "type": "integer",
      "label": "Number of jobs",
      "min": 4,
      "max": 10,
      "default": 6
    }
  },
  "tags": ["greedy", "scheduling", "jobs", "deadline"],
  "references": []
}
