{
  "schema_version": "1.0",
  "id": "dijkstra",
  "name": "Dijkstra's Shortest Path",
  "version": "1.0.0",
  "category": "graph",
  "visualization_type": "GRAPH",
  "entry_point": "algorithm.py",
  "benchmark_enabled": true,
  "description": "Finds the shortest path from a source node to all other nodes using a greedy priority queue.",
  "intuition": "Greedily settle the nearest unsettled node. Once settled, a node's distance is final — no edge can improve it. Edge weights on the graph show the cost of traversal.",
  "complexity": {
    "time_best": "O((V + E) log V)",
    "time_average": "O((V + E) log V)",
    "time_worst": "O((V + E) log V)",
    "space": "O(V)"
  },
  "parameters": {
    "node_count": {
      "type": "integer",
      "label": "Node count",
      "min": 4,
      "max": 10,
      "default": 7
    }
  },
  "tags": ["graph", "shortest-path", "greedy", "priority-queue", "weighted"],
  "references": [
    {
      "type": "article",
      "title": "Dijkstra's algorithm — Wikipedia",
      "url": "https://en.wikipedia.org/wiki/Dijkstra%27s_algorithm"
    }
  ]
}
