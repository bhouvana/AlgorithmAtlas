{
  "schema_version": "1.0",
  "id": "quickselect",
  "name": "Quickselect (Randomized)",
  "version": "1.0.0",
  "category": "randomized",
  "visualization_type": "ARRAY_BARS",
  "entry_point": "algorithm.py",
  "benchmark_enabled": true,
  "description": "Find the k-th smallest element in O(n) expected time using randomized partitioning.",
  "intuition": "Like quicksort: pick a random pivot and partition. But only recurse into the side that contains position k. Expected O(n) since we halve the problem on average.",
  "complexity": {
    "time_best": "O(n)",
    "time_average": "O(n)",
    "time_worst": "O(n²)",
    "space": "O(1)"
  },
  "parameters": {
    "array_size": {
      "type": "integer",
      "label": "Array size",
      "min": 6,
      "max": 20,
      "default": 12
    }
  },
  "tags": ["randomized", "quickselect", "order-statistics", "selection"],
  "references": []
}
