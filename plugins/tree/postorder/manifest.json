{
  "schema_version": "1.0",
  "id": "postorder-traversal",
  "name": "Postorder Traversal",
  "version": "1.0.0",
  "category": "tree",
  "visualization_type": "TREE",
  "entry_point": "algorithm.py",
  "benchmark_enabled": true,
  "description": "Visits both subtrees before the root: left → right → root.",
  "intuition": "Like cleaning up after a meeting: finish all the sub-tasks (subtrees) before signing off on the parent. Used when deleting a tree — you must delete children before the parent, otherwise you lose references to them.",
  "complexity": {
    "time_best": "O(n)",
    "time_average": "O(n)",
    "time_worst": "O(n)",
    "space": "O(h)"
  },
  "parameters": {
    "node_count": {
      "type": "integer",
      "label": "Node count",
      "min": 5,
      "max": 15,
      "default": 9
    }
  },
  "tags": ["tree", "traversal", "postorder", "dfs", "deletion"],
  "references": [
    {
      "type": "article",
      "title": "Tree Traversal — Wikipedia",
      "url": "https://en.wikipedia.org/wiki/Tree_traversal"
    }
  ]
}
