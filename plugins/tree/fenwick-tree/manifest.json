{
  "schema_version": "1.0",
  "id": "fenwick-tree",
  "name": "Fenwick Tree (BIT)",
  "version": "1.0.0",
  "category": "tree",
  "visualization_type": "MATRIX",
  "entry_point": "algorithm.py",
  "benchmark_enabled": true,
  "description": "Build a Binary Indexed Tree (Fenwick Tree) for O(log n) prefix-sum queries and point updates.",
  "intuition": "Each index i in the BIT stores the sum of a range whose size is determined by the lowest set bit of i (i & -i). Update propagates upward by adding the lowest bit; prefix query accumulates downward by subtracting it.",
  "complexity": {
    "time_best": "O(n) build, O(log n) query/update",
    "time_average": "O(n) build, O(log n) query/update",
    "time_worst": "O(n) build, O(log n) query/update",
    "space": "O(n)"
  },
  "parameters": {
    "array_size": {
      "type": "integer",
      "label": "Array size",
      "min": 4,
      "max": 10,
      "default": 8
    }
  },
  "tags": ["tree", "fenwick-tree", "bit", "prefix-sum", "data-structure"],
  "references": []
}
