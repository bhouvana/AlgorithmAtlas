[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "algorithm-atlas-rs"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "algorithm_atlas_rs"
python-source = "python"
