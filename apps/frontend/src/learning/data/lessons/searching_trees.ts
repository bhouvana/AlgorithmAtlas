import type { LessonData } from '../../lessons/GenericLesson';

export const searchingTreesLessons: Record<string, LessonData> = {
  'linear-search': {
    concept: {
      overview:
        'Linear search scans every element in a sequence from left to right, comparing each one to the target until a match is found or the end is reached. It is the simplest searching algorithm and requires no preprocessing or sorting of the data. Because it checks every element in the worst case, its time complexity is O(n).',
      keyPoints: [
        {
          title: 'No Preprocessing Required',
          desc: 'Linear search works directly on any sequence — sorted or unsorted — without any prior arrangement or index building.',
        },
        {
          title: 'Works on Any Sequence',
          desc: 'It is not limited to arrays; linear search can be applied to linked lists, streams, or any iterable collection.',
        },
        {
          title: 'Always O(n)',
          desc: 'Even in the average case the algorithm may inspect half the elements, giving O(n) time. Only the best case (target is first) is O(1).',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Start at Index 0',
        desc: 'Begin at the first element of the array. Set a pointer (or index i) to 0.',
      },
      {
        n: '2',
        label: 'Compare Current Element',
        desc: 'Check whether arr[i] equals the target value.',
      },
      {
        n: '3',
        label: 'Return on Match',
        desc: 'If arr[i] == target, return i — the index where the element was found.',
      },
      {
        n: '4',
        label: 'Advance the Pointer',
        desc: 'If there is no match, increment i by 1 and continue to the next element.',
      },
      {
        n: '5',
        label: 'Return -1 if Not Found',
        desc: 'If i reaches the end of the array without finding the target, return -1 to signal absence.',
      },
    ],
    codeExamples: [
      {
        label: 'Linear Search — iterative',
        code: [
          'def linear_search(arr, target):',
          '    for i in range(len(arr)):',
          '        if arr[i] == target:',
          '            return i',
          '    return -1',
          '',
          '# Example',
          'nums = [4, 2, 9, 7, 5]',
          'print(linear_search(nums, 7))  # 3',
          'print(linear_search(nums, 3))  # -1',
        ].join('\n'),
        complexity: 'Time O(n) | Space O(1)',
        variant: 'default',
      },
      {
        label: 'Linear Search — find all occurrences',
        code: [
          'def linear_search_all(arr, target):',
          '    results = []',
          '    for i in range(len(arr)):',
          '        if arr[i] == target:',
          '            results.append(i)',
          '    return results',
          '',
          'nums = [1, 3, 3, 7, 3]',
          'print(linear_search_all(nums, 3))  # [1, 2, 4]',
        ].join('\n'),
        complexity: 'Time O(n) | Space O(k) where k = occurrences',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Best Case', value: 'O(1)', color: 'text-emerald-400', note: 'Target is the first element' },
        { case: 'Average Case', value: 'O(n)', color: 'text-indigo-400', note: 'Target is somewhere in the middle on average' },
        { case: 'Worst Case', value: 'O(n)', color: 'text-indigo-400', note: 'Target is last or not present' },
        { case: 'Space', value: 'O(1)', color: 'text-emerald-400', note: 'Only a few index variables needed' },
      ],
      note: 'Linear search is the right tool when data is unsorted or very small (n < ~50), where sort overhead would dominate.',
    },
    realWorld: [
      { title: 'Searching Unsorted Lists', desc: 'When data arrives in an arbitrary order and sorting is not practical, linear search is the only option.' },
      { title: 'Finding an Item in an Array', desc: 'Standard library methods like Python\'s "in" operator and JavaScript\'s Array.includes() use linear search internally.' },
      { title: 'Checking Value Existence', desc: 'Verifying whether a value exists in a small collection before inserting or updating uses linear scan.' },
      { title: 'Small Datasets', desc: 'When n is small (e.g., a list of menu options), the overhead of sorting or hashing outweighs the O(n) scan cost.' },
    ],
    videoId: "246V51AWwZM",
    videoTitle: "Learn Linear Search in 3 Minutes",
    quiz: [
      {
        q: 'When is linear search preferred over binary search?',
        options: [
          'When the array is very large and sorted',
          'When the array is unsorted or too small to justify sorting',
          'When we need O(log n) performance',
          'When the array contains only integers',
        ],
        correct: 1,
        explanation: 'Linear search is preferred on unsorted data (binary search requires sorted input) or on very small arrays where sorting overhead is not worthwhile.',
      },
      {
        q: 'What is the best-case time complexity of linear search?',
        options: ['O(n)', 'O(log n)', 'O(1)', 'O(n²)'],
        correct: 2,
        explanation: 'The best case is O(1) when the target element happens to be the very first element checked.',
      },
      {
        q: 'Does linear search require the input array to be sorted?',
        options: [
          'Yes, it only works on sorted arrays',
          'No, it works on any sequence regardless of order',
          'Yes, but only for descending order',
          'It depends on the data type',
        ],
        correct: 1,
        explanation: 'Linear search compares elements one by one in sequence, so it works correctly on both sorted and unsorted data.',
      },
      {
        q: 'What is the average-case time complexity of linear search?',
        options: ['O(1)', 'O(log n)', 'O(n)', 'O(n²)'],
        correct: 2,
        explanation: 'On average, the target is found halfway through the array, requiring n/2 comparisons. This is still O(n) because we drop constant factors in Big-O notation.',
      },
      {
        q: 'Which of the following data structures can linear search be applied to that binary search cannot?',
        options: [
          'Sorted arrays',
          'Balanced binary search trees',
          'Singly linked lists with unsorted elements',
          'Hash tables',
        ],
        correct: 2,
        explanation: 'Binary search requires O(1) random access and sorted order, so it cannot be applied to unsorted linked lists. Linear search only needs sequential access, making it compatible with any iterable including linked lists.',
      },
      {
        q: 'How many comparisons does linear search make in the worst case on an array of n elements?',
        options: ['1', 'log₂ n', 'n/2', 'n'],
        correct: 3,
        explanation: 'In the worst case the target is the last element or is absent, so linear search inspects all n elements — making n comparisons total.',
      },
    ],
  },

  'ternary-search': {
    concept: {
      overview:
        'Ternary search divides a sorted array into three equal parts and determines which third contains the target, then recurses on that third. Like binary search but with a three-way split, it runs in O(log₃ n) = O(log n) time. It is also applicable to unimodal functions — continuous functions with a single peak — to find the maximum or minimum.',
      keyPoints: [
        {
          title: 'Three-Way Split',
          desc: 'Two midpoints (mid1 and mid2) divide the search space into three parts, reducing the range to one-third each iteration instead of one-half.',
        },
        {
          title: 'Works on Unimodal Functions',
          desc: 'Beyond sorted arrays, ternary search can find the maximum of a unimodal curve by comparing function values at mid1 and mid2.',
        },
        {
          title: 'Slightly Worse Than Binary in Practice',
          desc: 'Although O(log₃ n) sounds faster, each iteration performs 2 comparisons vs 1 for binary search, making binary search faster in most real-world scenarios.',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Compute mid1 and mid2',
        desc: 'mid1 = left + (right - left) / 3  and  mid2 = right - (right - left) / 3',
      },
      {
        n: '2',
        label: 'Check mid1',
        desc: 'If arr[mid1] == target, return mid1.',
      },
      {
        n: '3',
        label: 'Check mid2',
        desc: 'If arr[mid2] == target, return mid2.',
      },
      {
        n: '4',
        label: 'Recurse on Left Third',
        desc: 'If target < arr[mid1], the target must be in [left, mid1-1]. Recurse on that range.',
      },
      {
        n: '5',
        label: 'Recurse on Right Third',
        desc: 'If target > arr[mid2], the target must be in [mid2+1, right]. Recurse on that range.',
      },
      {
        n: '6',
        label: 'Recurse on Middle Third',
        desc: 'Otherwise the target lies in (mid1, mid2). Recurse on [mid1+1, mid2-1].',
      },
    ],
    codeExamples: [
      {
        label: 'Ternary Search — recursive on sorted array',
        code: [
          'def ternary_search(arr, target, left, right):',
          '    if left > right:',
          '        return -1',
          '    mid1 = left + (right - left) // 3',
          '    mid2 = right - (right - left) // 3',
          '    if arr[mid1] == target:',
          '        return mid1',
          '    if arr[mid2] == target:',
          '        return mid2',
          '    if target < arr[mid1]:',
          '        return ternary_search(arr, target, left, mid1 - 1)',
          '    elif target > arr[mid2]:',
          '        return ternary_search(arr, target, mid2 + 1, right)',
          '    else:',
          '        return ternary_search(arr, target, mid1 + 1, mid2 - 1)',
          '',
          'arr = [1, 3, 5, 7, 9, 11, 13]',
          'print(ternary_search(arr, 7, 0, len(arr)-1))  # 3',
        ].join('\n'),
        complexity: 'Time O(log₃ n) | Space O(log n) recursive stack',
        variant: 'default',
      },
      {
        label: 'Ternary Search on Unimodal Function',
        code: [
          'def find_max_unimodal(f, left, right, eps=1e-9):',
          '    """Find x in [left, right] maximising unimodal f(x)."""',
          '    while right - left > eps:',
          '        m1 = left + (right - left) / 3',
          '        m2 = right - (right - left) / 3',
          '        if f(m1) < f(m2):',
          '            left = m1',
          '        else:',
          '            right = m2',
          '    return (left + right) / 2',
          '',
          '# f(x) = -(x-3)^2 + 9 peaks at x=3',
          'f = lambda x: -(x - 3)**2 + 9',
          'print(find_max_unimodal(f, 0, 6))  # ~3.0',
        ].join('\n'),
        complexity: 'Time O(log((right-left)/eps)) | Space O(1)',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Best Case', value: 'O(1)', color: 'text-emerald-400', note: 'Target found at mid1 or mid2 on first call' },
        { case: 'Average / Worst', value: 'O(log n)', color: 'text-cyan-400', note: 'log₃ n = log n / log 3 ≈ 0.63 log₂ n levels but 2× comparisons each' },
        { case: 'Space (recursive)', value: 'O(log n)', color: 'text-cyan-400', note: 'Call stack depth proportional to log₃ n' },
      ],
      note: 'In practice binary search is faster because it uses 1 comparison per level vs 2 for ternary search.',
    },
    realWorld: [
      { title: 'Unimodal Function Optimisation', desc: 'Numerical methods use ternary search to locate the maximum or minimum of a single-peaked curve without a closed-form derivative.' },
      { title: 'Competitive Programming', desc: 'Many contest problems reduce to finding the peak of a convex/concave score function, where ternary search gives an elegant O(log n) solution.' },
      { title: 'Numerical Analysis', desc: 'Root-finding and optimisation routines in scientific computing apply ternary search over continuous intervals.' },
      { title: 'Game AI', desc: 'Some adversarial search evaluations model a score function that rises then falls with search depth; ternary search finds the sweet spot.' },
    ],
    videoId: "o3HPRpbGlbI",
    videoTitle: "Ternary Search in 4 Minutes (Python)",
    quiz: [
      {
        q: 'How does ternary search compare to binary search in practice?',
        options: [
          'Ternary search is always faster because it reduces the range more',
          'They are identical in performance',
          'Binary search is usually faster because ternary search does 2 comparisons per iteration vs 1',
          'Ternary search is faster only on linked lists',
        ],
        correct: 2,
        explanation: 'Although ternary search reduces the range to one-third each step, it performs 2 comparisons per iteration. Binary search does 1 comparison and reduces to half, resulting in fewer total comparisons.',
      },
      {
        q: 'What is a unimodal function?',
        options: [
          'A function that only takes integer values',
          'A function with exactly one peak (or one valley) — it increases then decreases (or vice versa)',
          'A function defined over exactly three intervals',
          'A function that is always monotonically increasing',
        ],
        correct: 1,
        explanation: 'A unimodal function has a single maximum (or minimum). It strictly increases up to that point then strictly decreases, making ternary search applicable to find the extremum.',
      },
      {
        q: 'Does ternary search require sorted input?',
        options: [
          'No, it works on any array',
          'Yes, or the array must be unimodal depending on the use case',
          'Yes, but only descending order',
          'No, it randomly picks elements',
        ],
        correct: 1,
        explanation: 'For array element search the array must be sorted. For function optimisation the function must be unimodal. In both cases a structural property (monotonicity or single peak) is required.',
      },
      {
        q: 'What is the worst-case number of comparisons ternary search makes on an array of n elements?',
        options: ['4 log₃ n', '2 log₃ n', 'log₂ n', 'n / 3'],
        correct: 1,
        explanation: 'Each iteration of ternary search makes 2 comparisons (check mid1, check mid2) and reduces the range to one-third. After at most log₃ n iterations, the range is exhausted — giving 2 log₃ n ≈ 1.26 log₂ n total comparisons.',
      },
      {
        q: 'In ternary search on a sorted array, how are mid1 and mid2 computed?',
        options: [
          'mid1 = left + (right - left) / 2,  mid2 = mid1 + 1',
          'mid1 = left + (right - left) / 3,  mid2 = right - (right - left) / 3',
          'mid1 = left,  mid2 = right',
          'mid1 = (left + right) / 4,  mid2 = 3 * (left + right) / 4',
        ],
        correct: 1,
        explanation: 'Ternary search divides [left, right] into three equal thirds using mid1 = left + (right - left) / 3 and mid2 = right - (right - left) / 3. The target is in one of the three resulting sub-ranges.',
      },
      {
        q: 'Which algorithmic technique does ternary search on a unimodal function exemplify?',
        options: [
          'Dynamic programming',
          'Greedy algorithms',
          'Divide and conquer',
          'Backtracking',
        ],
        correct: 2,
        explanation: 'Ternary search is a divide-and-conquer algorithm: it splits the search space into three parts, determines which part must contain the answer, and recurses on that sub-problem — halving context with each call.',
      },
    ],
  },

  'interpolation-search': {
    concept: {
      overview:
        'Interpolation search estimates the likely position of the target based on its value relative to the range of the array, similar to how you might open a dictionary near the letter "S" rather than flipping to the middle. It uses the probe formula pos = low + (target - arr[low]) * (high - low) / (arr[high] - arr[low]). On uniformly distributed data it achieves O(log log n) average time, but degrades to O(n) on clustered or skewed data.',
      keyPoints: [
        {
          title: 'Probe Formula',
          desc: 'pos = low + (target - arr[low]) / (arr[high] - arr[low]) * (high - low) estimates where the target would be if values are evenly spaced.',
        },
        {
          title: 'Beats Binary Search on Uniform Data',
          desc: 'When values are approximately uniformly distributed, interpolation search converges in O(log log n) — dramatically faster than O(log n).',
        },
        {
          title: 'O(n) Worst Case on Clustered Data',
          desc: 'If values are heavily skewed (e.g., many equal or exponentially spaced values), the probe can be off and degrade to O(n) comparisons.',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Calculate Estimated Position',
        desc: 'Use the interpolation formula: pos = low + (target - arr[low]) * (high - low) / (arr[high] - arr[low]).',
      },
      {
        n: '2',
        label: 'Check if Element Matches',
        desc: 'If arr[pos] == target, return pos.',
      },
      {
        n: '3',
        label: 'Narrow to Upper Half',
        desc: 'If arr[pos] < target, the target is to the right. Set low = pos + 1.',
      },
      {
        n: '4',
        label: 'Narrow to Lower Half',
        desc: 'If arr[pos] > target, the target is to the left. Set high = pos - 1.',
      },
      {
        n: '5',
        label: 'Recurse or Loop on Narrowed Range',
        desc: 'Repeat the probe calculation on the narrowed [low, high] range until found or low > high.',
      },
    ],
    codeExamples: [
      {
        label: 'Interpolation Search — iterative',
        code: [
          'def interpolation_search(arr, target):',
          '    low, high = 0, len(arr) - 1',
          '    while low <= high and arr[low] <= target <= arr[high]:',
          '        if low == high:',
          '            return low if arr[low] == target else -1',
          '        # Interpolation probe',
          '        pos = low + (target - arr[low]) * (high - low) // (arr[high] - arr[low])',
          '        if arr[pos] == target:',
          '            return pos',
          '        elif arr[pos] < target:',
          '            low = pos + 1',
          '        else:',
          '            high = pos - 1',
          '    return -1',
          '',
          'arr = [10, 20, 30, 40, 50, 60, 70]',
          'print(interpolation_search(arr, 40))  # 3',
          'print(interpolation_search(arr, 25))  # -1',
        ].join('\n'),
        complexity: 'Time O(log log n) avg uniform | Space O(1)',
        variant: 'default',
      },
      {
        label: 'Why Probe Formula Works (annotated)',
        code: [
          '# The probe formula is linear interpolation:',
          '#   fraction = (target - arr[low]) / (arr[high] - arr[low])',
          '#   pos      = low + fraction * (high - low)',
          '',
          '# Example: arr = [10, 20, 30, 40, 50], target = 30',
          '# low=0 arr[low]=10, high=4 arr[high]=50',
          '# fraction = (30-10) / (50-10) = 20/40 = 0.5',
          '# pos = 0 + 0.5 * 4 = 2  -> arr[2] = 30  HIT on first probe!',
          '',
          '# Uniform data: each probe lands close to target.',
          '# Skewed data : fraction is far off -> many probes needed.',
        ].join('\n'),
        complexity: 'Best O(1) on perfectly uniform data',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Average (uniform data)', value: 'O(log log n)', color: 'text-cyan-400', note: 'Each probe squares the convergence rate' },
        { case: 'Worst Case (clustered)', value: 'O(n)', color: 'text-indigo-400', note: 'Badly skewed distribution causes linear scan' },
        { case: 'Best Case', value: 'O(1)', color: 'text-emerald-400', note: 'Target found on first probe' },
        { case: 'Space', value: 'O(1)', color: 'text-emerald-400', note: 'Only a few variables (low, high, pos)' },
      ],
      note: 'Use interpolation search when you know data is uniformly distributed; otherwise binary search is safer.',
    },
    realWorld: [
      { title: 'Phone Book / Dictionary Lookup', desc: 'Looking up "Smith" means opening near the back rather than the middle — exactly the intuition behind interpolation search.' },
      { title: 'Sorted Database Columns', desc: 'Database engines may use interpolation estimates for range scans on numeric columns (e.g., timestamps or IDs) that are near-uniformly distributed.' },
      { title: 'Numerical Tables', desc: 'Scientific lookup tables with evenly spaced x-values allow extremely fast probing via interpolation.' },
      { title: 'Log File Analysis', desc: 'Searching for a specific timestamp in a uniformly distributed log file benefits from interpolation since timestamps grow at a roughly constant rate.' },
    ],
    videoId: "IjrF0Cazy-k",
    videoTitle: "Interpolation Search Algorithm | Explanation, Working & Code",
    quiz: [
      {
        q: 'When does interpolation search outperform binary search?',
        options: [
          'Always — it is a strictly better algorithm',
          'When the data is uniformly distributed, giving O(log log n) average time',
          'When the array is unsorted',
          'When the array contains strings',
        ],
        correct: 1,
        explanation: 'Interpolation search achieves O(log log n) on uniformly distributed sorted data because each probe lands very close to the target. On non-uniform data it can degrade to O(n).',
      },
      {
        q: 'What is the interpolation probe formula?',
        options: [
          'pos = (low + high) / 2',
          'pos = low + (target - arr[low]) * (high - low) / (arr[high] - arr[low])',
          'pos = high - (arr[high] - target)',
          'pos = low * high / target',
        ],
        correct: 1,
        explanation: 'The formula linearly interpolates the expected index based on where target falls in the value range [arr[low], arr[high]], analogous to dictionary lookup.',
      },
      {
        q: 'What is the worst-case time complexity of interpolation search?',
        options: ['O(log log n)', 'O(log n)', 'O(n)', 'O(n²)'],
        correct: 2,
        explanation: 'On heavily skewed or clustered data the probe consistently misses and the algorithm degrades to O(n) — worse than binary search\'s guaranteed O(log n).',
      },
      {
        q: 'What precondition must the input satisfy for interpolation search to work correctly?',
        options: [
          'The array must contain only positive integers',
          'The array must be sorted in ascending order',
          'The array must have an even number of elements',
          'The array must not contain duplicate values',
        ],
        correct: 1,
        explanation: 'Interpolation search, like binary search, requires the array to be sorted in ascending order. The probe formula also assumes numeric keys so it can linearly interpolate the estimated position.',
      },
      {
        q: 'Why can the interpolation probe formula divide by zero, and how is it typically handled?',
        options: [
          'It cannot divide by zero because arrays always have at least two distinct elements',
          'When arr[high] == arr[low] all remaining elements are equal; check if any equals the target and return accordingly',
          'Division by zero is prevented by always keeping low < high',
          'The formula uses integer division so zero is replaced by 1 automatically',
        ],
        correct: 1,
        explanation: 'If arr[high] == arr[low], the denominator (arr[high] - arr[low]) is zero. This means all elements in [low, high] are equal, so we simply check whether arr[low] equals the target and return the result directly.',
      },
      {
        q: 'Interpolation search is analogous to which everyday activity?',
        options: [
          'Flipping to the exact middle of a book every time',
          'Reading a book from cover to cover',
          'Opening a dictionary near the alphabetical position of the word you seek',
          'Using the table of contents to jump to a chapter',
        ],
        correct: 2,
        explanation: 'Just as you open a dictionary near "S" when searching for "Smith" rather than always starting at the middle, interpolation search estimates the probable index of the target based on its value relative to the range extremes.',
      },
    ],
  },

  'binary-trees': {
    concept: {
      overview:
        'A binary tree is a hierarchical data structure in which each node has at most two children, conventionally called the left child and the right child. Nodes are connected by edges representing parent-child relationships. Binary trees are the foundation for more specialised structures like binary search trees, heaps, and expression trees.',
      keyPoints: [
        {
          title: 'Parent-Child Relationships',
          desc: 'Every node except the root has exactly one parent. The root is the topmost node with no parent. Nodes with no children are called leaves.',
        },
        {
          title: 'Height and Balance',
          desc: 'A perfectly balanced binary tree of n nodes has height h = ⌊log₂ n⌋. A degenerate (skewed) tree where every node has only one child degrades to a linked list with height n - 1.',
        },
        {
          title: 'Three Traversal Orders',
          desc: 'In-order (left → root → right), pre-order (root → left → right), and post-order (left → right → root) visit every node exactly once in O(n).',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Root is the Top Node',
        desc: 'The root node is the entry point of the tree. Every traversal or search starts here.',
      },
      {
        n: '2',
        label: 'At Most Two Children',
        desc: 'Each node holds a value (data) plus optional left and right child pointers (or None).',
      },
      {
        n: '3',
        label: 'In-Order Traversal (Left → Root → Right)',
        desc: 'Recursively visit the left subtree, then the current node, then the right subtree. For a BST this yields sorted order.',
      },
      {
        n: '4',
        label: 'Pre-Order Traversal (Root → Left → Right)',
        desc: 'Visit the current node first, then recurse left and right. Used to copy or serialize a tree.',
      },
      {
        n: '5',
        label: 'Compute Height and Depth',
        desc: 'Height = longest root-to-leaf path (recursive: 1 + max(height(left), height(right))). Depth of a node = distance from root.',
      },
    ],
    codeExamples: [
      {
        label: 'TreeNode class and in-order traversal',
        code: [
          'class TreeNode:',
          '    def __init__(self, val=0):',
          '        self.val   = val',
          '        self.left  = None',
          '        self.right = None',
          '',
          'def inorder(root):',
          '    if root is None:',
          '        return []',
          '    return inorder(root.left) + [root.val] + inorder(root.right)',
          '',
          '# Build a simple tree:',
          '#       4',
          '#      / \\',
          '#     2   6',
          '#    / \\',
          '#   1   3',
          'root = TreeNode(4)',
          'root.left  = TreeNode(2)',
          'root.right = TreeNode(6)',
          'root.left.left  = TreeNode(1)',
          'root.left.right = TreeNode(3)',
          'print(inorder(root))  # [1, 2, 3, 4, 6]',
        ].join('\n'),
        complexity: 'Traversal O(n) | Space O(h) call stack',
        variant: 'default',
      },
      {
        label: 'Tree height (recursive)',
        code: [
          'def height(root):',
          '    if root is None:',
          '        return 0',
          '    return 1 + max(height(root.left), height(root.right))',
          '',
          'def count_nodes(root):',
          '    if root is None:',
          '        return 0',
          '    return 1 + count_nodes(root.left) + count_nodes(root.right)',
          '',
          'print(height(root))       # 3',
          'print(count_nodes(root))  # 5',
        ].join('\n'),
        complexity: 'Both O(n) time | O(h) space',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Access / Search (balanced)', value: 'O(log n)', color: 'text-cyan-400', note: 'Height is log n for a balanced tree' },
        { case: 'Access / Search (unbalanced)', value: 'O(n)', color: 'text-indigo-400', note: 'Degenerate tree looks like a linked list' },
        { case: 'Traversal (any order)', value: 'O(n)', color: 'text-indigo-400', note: 'Every node visited exactly once' },
        { case: 'Space', value: 'O(n)', color: 'text-indigo-400', note: 'n nodes stored; O(h) extra for recursion stack' },
      ],
      note: 'Without balancing, insert patterns can create degenerate trees; use AVL or Red-Black trees to guarantee O(log n).',
    },
    realWorld: [
      { title: 'File System Directories', desc: 'A directory tree is a rooted tree where folders are internal nodes and files are leaves. Traversal mirrors DFS directory listing.' },
      { title: 'HTML DOM Tree', desc: 'Web browsers represent HTML as a tree of nodes. JavaScript DOM manipulation traverses and modifies this binary (or n-ary) tree structure.' },
      { title: 'Expression Parsing', desc: 'Arithmetic expressions like (a + b) * c are stored as expression trees where leaves are operands and internal nodes are operators.' },
      { title: 'Decision Trees in Machine Learning', desc: 'Decision tree classifiers partition data by asking binary questions at each node, forming a binary tree that maps features to predictions.' },
    ],
    videoId: "H5JubkIy_p8",
    videoTitle: "Data Structures: Binary Tree",
    quiz: [
      {
        q: 'What is the height of a balanced binary tree containing n nodes?',
        options: ['O(n)', 'O(n²)', 'O(log n)', 'O(1)'],
        correct: 2,
        explanation: 'A balanced binary tree distributes nodes evenly between left and right subtrees, giving height ⌊log₂ n⌋ = O(log n).',
      },
      {
        q: 'What does in-order traversal visit in the order left → root → right?',
        options: [
          'It visits nodes top-down level by level',
          'It visits nodes left subtree first, then current node, then right subtree',
          'It visits nodes right-to-left at each level',
          'It visits only leaf nodes',
        ],
        correct: 1,
        explanation: 'In-order traversal recursively visits the left subtree first, then processes the current node, then visits the right subtree. Applied to a BST it produces sorted output.',
      },
      {
        q: 'What makes a binary tree "degenerate"?',
        options: [
          'It has more than two children per node',
          'Every node has exactly one child, making the tree equivalent to a linked list',
          'The root has no children',
          'The tree has an odd number of nodes',
        ],
        correct: 1,
        explanation: 'A degenerate (skewed) tree occurs when insertions are in sorted order into a BST, causing every node to have only a right (or only a left) child, giving O(n) height and search time.',
      },
      {
        q: 'How many nodes does a full binary tree of height h have at most?',
        options: ['h', '2h', '2^h - 1', '2^(h+1) - 1'],
        correct: 3,
        explanation: 'A full (perfect) binary tree of height h has 2^(h+1) - 1 nodes in total. Each level k contributes 2^k nodes, and summing levels 0 through h gives 2^(h+1) - 1.',
      },
      {
        q: 'Which traversal order would you use to produce a copy of a binary tree (clone it)?',
        options: [
          'In-order (left → root → right)',
          'Post-order (left → right → root)',
          'Pre-order (root → left → right)',
          'Level-order (BFS)',
        ],
        correct: 2,
        explanation: 'Pre-order traversal visits the root before its children. This mirrors the natural construction order — create the current node first, then recursively create left and right subtrees — making it ideal for cloning or serializing a tree.',
      },
      {
        q: 'What is the minimum number of nodes in a binary tree of height h?',
        options: ['1', 'h', 'h + 1', '2h'],
        correct: 2,
        explanation: 'A binary tree of height h must have at least h + 1 nodes — one node per level in the worst-case degenerate (skewed) tree where each level has exactly one node.',
      },
    ],
  },

  'binary-search-trees': {
    concept: {
      overview:
        'A Binary Search Tree (BST) is a binary tree that maintains the BST invariant: for every node, all values in its left subtree are strictly less than the node value, and all values in its right subtree are strictly greater. This property enables efficient O(log n) average-case search, insertion, and deletion by eliminating half the remaining nodes at each step.',
      keyPoints: [
        {
          title: 'BST Invariant',
          desc: 'left subtree values < node value < right subtree values at every node. This must hold recursively for all subtrees.',
        },
        {
          title: 'In-Order Traversal Yields Sorted Output',
          desc: 'Visiting a BST in-order (left → root → right) produces all elements in ascending sorted order — a key property.',
        },
        {
          title: 'O(n) Worst Case Without Balancing',
          desc: 'Inserting sorted data produces a degenerate right-skewed tree with O(n) height, destroying the O(log n) advantage. Self-balancing variants (AVL, Red-Black) prevent this.',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Search',
        desc: 'Compare target with current node. If equal, found. If smaller, go left. If larger, go right. Repeat until found or null.',
      },
      {
        n: '2',
        label: 'Insert',
        desc: 'Perform a search until reaching a null pointer, then place the new node there. The BST invariant is maintained automatically.',
      },
      {
        n: '3',
        label: 'Delete — Leaf Node',
        desc: 'If the node to delete has no children, simply remove it by setting the parent pointer to null.',
      },
      {
        n: '4',
        label: 'Delete — One Child',
        desc: 'If the node has exactly one child, replace the node with its child — splice it out.',
      },
      {
        n: '5',
        label: 'Delete — Two Children',
        desc: 'Find the in-order successor (smallest node in right subtree). Copy its value to the current node, then delete the successor.',
      },
    ],
    codeExamples: [
      {
        label: 'BST — search and insert',
        code: [
          'class BST:',
          '    def __init__(self):',
          '        self.root = None',
          '',
          '    def insert(self, val):',
          '        def _insert(node, val):',
          '            if node is None:',
          '                return TreeNode(val)',
          '            if val < node.val:',
          '                node.left  = _insert(node.left,  val)',
          '            elif val > node.val:',
          '                node.right = _insert(node.right, val)',
          '            return node  # duplicate: ignore',
          '        self.root = _insert(self.root, val)',
          '',
          '    def search(self, val):',
          '        node = self.root',
          '        while node:',
          '            if   val == node.val: return True',
          '            elif val <  node.val: node = node.left',
          '            else:                 node = node.right',
          '        return False',
          '',
          'bst = BST()',
          'for v in [5, 3, 7, 1, 4]:',
          '    bst.insert(v)',
          'print(bst.search(4))  # True',
          'print(bst.search(6))  # False',
        ].join('\n'),
        complexity: 'Insert/Search O(log n) avg | O(n) worst',
        variant: 'default',
      },
      {
        label: 'BST — in-order traversal gives sorted sequence',
        code: [
          'def inorder_bst(root, result=None):',
          '    if result is None:',
          '        result = []',
          '    if root:',
          '        inorder_bst(root.left,  result)',
          '        result.append(root.val)',
          '        inorder_bst(root.right, result)',
          '    return result',
          '',
          '# After inserting [5, 3, 7, 1, 4]:',
          'print(inorder_bst(bst.root))  # [1, 3, 4, 5, 7]',
        ].join('\n'),
        complexity: 'O(n) time | O(h) space',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Search / Insert / Delete (avg)', value: 'O(log n)', color: 'text-cyan-400', note: 'Balanced tree; height ≈ log n' },
        { case: 'Search / Insert / Delete (worst)', value: 'O(n)', color: 'text-indigo-400', note: 'Sorted insertions create a degenerate tree' },
        { case: 'In-Order Traversal', value: 'O(n)', color: 'text-indigo-400', note: 'Every node visited once' },
        { case: 'Space', value: 'O(n)', color: 'text-indigo-400', note: 'n nodes; O(h) extra for recursive call stack' },
      ],
      note: 'The expected height for random insertions is O(log n), but adversarial input degrades to O(n). Use AVL or Red-Black trees in production.',
    },
    realWorld: [
      { title: 'Database Indexing', desc: 'B-trees (generalisations of BSTs) power relational database indexes, enabling O(log n) lookups on primary and secondary keys.' },
      { title: 'Symbol Tables in Compilers', desc: 'Compilers store variable names and their attributes in BST-based symbol tables for fast lookup and insertion during parsing.' },
      { title: 'Set and Map Data Structures', desc: 'C++ std::set and std::map, Java TreeSet and TreeMap are backed by balanced BSTs (typically Red-Black trees).' },
      { title: 'Auto-Complete Systems', desc: 'BSTs and their balanced variants support lexicographic ordering, enabling efficient prefix search for auto-complete features.' },
    ],
    videoId: "Gt2yBZAhsGM",
    videoTitle: "Learn Binary Search Trees in 20 Minutes",
    quiz: [
      {
        q: 'What is the BST property that every BST must maintain?',
        options: [
          'All left children must be even and right children must be odd',
          'For every node, left subtree values < node value < right subtree values',
          'The tree must always be perfectly balanced',
          'Every node must have exactly two children',
        ],
        correct: 1,
        explanation: 'The BST invariant requires that all values in a node\'s left subtree are strictly less than the node\'s value, and all values in the right subtree are strictly greater. This holds recursively.',
      },
      {
        q: 'What does in-order traversal of a BST produce?',
        options: [
          'A random permutation of values',
          'Values in descending (largest first) order',
          'Values in ascending (sorted) order',
          'Only the leaf node values',
        ],
        correct: 2,
        explanation: 'In-order traversal (left → root → right) of a BST visits nodes in ascending order because all left subtree values are smaller and all right subtree values are larger.',
      },
      {
        q: 'When does a BST degenerate to O(n) operations?',
        options: [
          'When n is larger than 1000',
          'When values are inserted in sorted (or reverse-sorted) order',
          'When the tree has more than two children per node',
          'When duplicate values exist',
        ],
        correct: 1,
        explanation: 'Inserting values in sorted order (e.g., 1, 2, 3, 4, 5) causes every node to have only a right child, creating a linked-list-like structure of height n with O(n) search time.',
      },
      {
        q: 'When deleting a node with two children from a BST, which replacement strategy preserves the BST property?',
        options: [
          'Replace with the root of the left subtree',
          'Replace with the in-order predecessor or in-order successor',
          'Replace with a randomly chosen leaf',
          'Replace with the parent node',
        ],
        correct: 1,
        explanation: 'To delete a node with two children, copy the value of its in-order successor (smallest value in the right subtree) or in-order predecessor (largest in the left subtree) into the node, then delete that successor/predecessor. This always maintains the BST invariant.',
      },
      {
        q: 'What is the average-case height of a BST built by inserting n random values?',
        options: ['O(1)', 'O(log n)', 'O(n)', 'O(n log n)'],
        correct: 1,
        explanation: 'When keys are inserted in random order, the expected height of a BST is O(log n) — specifically about 4.31 log₂ n. Only adversarial (sorted) input causes O(n) height.',
      },
      {
        q: 'Which operation is NOT typically O(log n) in a balanced BST?',
        options: [
          'Searching for a value',
          'Inserting a new value',
          'Deleting a value',
          'Printing all values in sorted order',
        ],
        correct: 3,
        explanation: 'Printing all values in sorted order via in-order traversal visits every node exactly once, taking O(n) time. Search, insert, and delete all take O(log n) in a balanced BST because they traverse one root-to-leaf path.',
      },
    ],
  },

  'avl-trees': {
    concept: {
      overview:
        'AVL trees are self-balancing binary search trees invented by Adelson-Velsky and Landis in 1962. They maintain the balance invariant that the heights of the left and right subtrees of every node differ by at most 1 (balance factor ∈ {-1, 0, 1}). When an insertion or deletion violates this invariant, the tree restores balance through one or two rotations, guaranteeing O(log n) for all operations.',
      keyPoints: [
        {
          title: 'Balance Factor',
          desc: 'For every node, balance factor = height(left subtree) - height(right subtree). AVL enforces this is always in {-1, 0, 1}.',
        },
        {
          title: 'Four Rotation Cases',
          desc: 'Imbalance is fixed by rotations: LL (right rotate), RR (left rotate), LR (left rotate on child then right rotate on node), RL (right rotate on child then left rotate on node).',
        },
        {
          title: 'Guaranteed O(log n)',
          desc: 'By keeping the height strictly O(log n), all BST operations run in O(log n) worst case — unlike plain BSTs that can degrade to O(n).',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Insert as in BST',
        desc: 'Perform a standard BST insertion to place the new node at the correct leaf position.',
      },
      {
        n: '2',
        label: 'Update Heights Bottom-Up',
        desc: 'After insertion, update the height of each ancestor node on the path back to the root.',
      },
      {
        n: '3',
        label: 'Compute Balance Factor',
        desc: 'At each ancestor, compute balance_factor = height(left) - height(right).',
      },
      {
        n: '4',
        label: 'Identify Rotation Case',
        desc: 'If |balance_factor| > 1, determine which of the four cases applies (LL, RR, LR, RL) based on where the imbalance occurred.',
      },
      {
        n: '5',
        label: 'Perform Rotation(s)',
        desc: 'LL: single right rotation. RR: single left rotation. LR: left rotate child then right rotate current. RL: right rotate child then left rotate current.',
      },
    ],
    codeExamples: [
      {
        label: 'AVL Tree — height, balance factor, and rotations',
        code: [
          'class AVLNode:',
          '    def __init__(self, val):',
          '        self.val    = val',
          '        self.left   = None',
          '        self.right  = None',
          '        self.height = 1',
          '',
          'def get_height(n): return n.height if n else 0',
          'def get_bf(n):     return get_height(n.left) - get_height(n.right) if n else 0',
          '',
          'def right_rotate(y):',
          '    x, T2 = y.left, y.left.right',
          '    x.right, y.left = y, T2',
          '    y.height = 1 + max(get_height(y.left),  get_height(y.right))',
          '    x.height = 1 + max(get_height(x.left),  get_height(x.right))',
          '    return x',
          '',
          'def left_rotate(x):',
          '    y, T2 = x.right, x.right.left',
          '    y.left, x.right = x, T2',
          '    x.height = 1 + max(get_height(x.left),  get_height(x.right))',
          '    y.height = 1 + max(get_height(y.left),  get_height(y.right))',
          '    return y',
        ].join('\n'),
        complexity: 'Rotation O(1) | Height update O(log n) path',
        variant: 'default',
      },
      {
        label: 'AVL Insert with rebalancing',
        code: [
          'def avl_insert(root, val):',
          '    if not root: return AVLNode(val)',
          '    if val < root.val:   root.left  = avl_insert(root.left,  val)',
          '    elif val > root.val: root.right = avl_insert(root.right, val)',
          '    else: return root  # duplicate',
          '',
          '    root.height = 1 + max(get_height(root.left), get_height(root.right))',
          '    bf = get_bf(root)',
          '',
          '    # LL case',
          '    if bf > 1 and val < root.left.val:   return right_rotate(root)',
          '    # RR case',
          '    if bf < -1 and val > root.right.val: return left_rotate(root)',
          '    # LR case',
          '    if bf > 1 and val > root.left.val:',
          '        root.left = left_rotate(root.left)',
          '        return right_rotate(root)',
          '    # RL case',
          '    if bf < -1 and val < root.right.val:',
          '        root.right = right_rotate(root.right)',
          '        return left_rotate(root)',
          '    return root',
        ].join('\n'),
        complexity: 'Insert O(log n) | Rotations O(1)',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Search', value: 'O(log n)', color: 'text-cyan-400', note: 'Height guaranteed O(log n)' },
        { case: 'Insert', value: 'O(log n)', color: 'text-cyan-400', note: 'BST insert + O(log n) rebalancing path' },
        { case: 'Delete', value: 'O(log n)', color: 'text-cyan-400', note: 'BST delete + O(log n) rebalancing path' },
        { case: 'Space', value: 'O(n)', color: 'text-indigo-400', note: 'n nodes; O(h) = O(log n) stack for recursion' },
      ],
      note: 'AVL trees guarantee height ≤ 1.44 log₂(n+2), making all operations strictly O(log n) even in the worst case.',
    },
    realWorld: [
      { title: 'Database Indexes with Frequent Mutations', desc: 'When an index undergoes many insertions and deletions, AVL trees maintain balanced structure to ensure consistent query performance.' },
      { title: 'File System B-Trees', desc: 'Disk-based B-trees used in file systems are generalisations of AVL-style balanced trees, keeping tree height minimal to reduce disk I/O.' },
      { title: 'Priority Scheduling', desc: 'Operating system schedulers use balanced trees to manage process queues where priorities change dynamically.' },
      { title: 'Order Statistics', desc: 'Augmented AVL trees support finding the k-th smallest element in O(log n), useful in rank-based data retrieval.' },
    ],
    videoId: "CVA85JuJEn0",
    videoTitle: "AVL Trees Tutorial | Self Balancing Binary Search Trees",
    quiz: [
      {
        q: 'What is the balance factor of an AVL node?',
        options: [
          'The number of children minus the number of parents',
          'height(left subtree) - height(right subtree)',
          'The total number of nodes in both subtrees',
          'The depth of the node from the root',
        ],
        correct: 1,
        explanation: 'The balance factor = height(left subtree) - height(right subtree). AVL trees require this to be in {-1, 0, 1} for every node. A value outside this range triggers a rotation.',
      },
      {
        q: 'How many rotation types does AVL rebalancing use?',
        options: ['1 (only right rotation)', '2 (left and right)', '4 (LL, RR, LR, RL)', '8 (one per height level)'],
        correct: 2,
        explanation: 'AVL rebalancing covers four cases: LL (single right rotate), RR (single left rotate), LR (double: left then right), and RL (double: right then left).',
      },
      {
        q: 'What height does an AVL tree of n nodes guarantee?',
        options: ['O(n)', 'O(n log n)', 'O(log n)', 'O(1)'],
        correct: 2,
        explanation: 'AVL trees guarantee height ≤ 1.44 log₂(n+2), which is O(log n). This ensures all search, insert, and delete operations run in O(log n) worst case.',
      },
      {
        q: 'Which rotation case requires two rotations in an AVL tree?',
        options: [
          'LL case (left-left imbalance)',
          'RR case (right-right imbalance)',
          'LR case (left-right imbalance)',
          'Both LL and RR cases',
        ],
        correct: 2,
        explanation: 'The LR case (and its mirror RL case) requires two rotations: first a left rotation on the left child to convert it to an LL case, then a right rotation on the current node. The LL and RR cases each require only a single rotation.',
      },
      {
        q: 'After inserting a node into an AVL tree, at most how many rotations are needed to restore balance?',
        options: ['1', '2', 'O(log n)', 'O(n)'],
        correct: 1,
        explanation: 'Inserting a node into an AVL tree requires at most 2 rotations (a double rotation in the LR or RL case) to restore balance. After those rotations the heights above the rebalanced subtree are restored, so no further rotations are needed.',
      },
      {
        q: 'How does AVL tree performance compare to a Red-Black tree in practice?',
        options: [
          'AVL trees are always faster for all operations',
          'AVL trees have faster lookups but Red-Black trees have faster insertions and deletions due to fewer rotations',
          'Red-Black trees are always faster for all operations',
          'They have identical performance for all operations',
        ],
        correct: 1,
        explanation: 'AVL trees are more strictly balanced than Red-Black trees, giving slightly faster lookup. However, Red-Black trees require at most 2 rotations for insertion and 3 for deletion (vs O(log n) rotations possible in AVL deletion), making mutations faster in practice.',
      },
    ],
  },

  'heaps': {
    concept: {
      overview:
        'A heap is a complete binary tree that satisfies the heap property: in a max-heap every parent is greater than or equal to its children, and in a min-heap every parent is less than or equal to its children. Heaps are typically stored as arrays where parent-child relationships are computed by index arithmetic, enabling O(1) peek of the min/max element and O(log n) insert and extract.',
      keyPoints: [
        {
          title: 'Array Representation',
          desc: 'With 1-based indexing, root is at index 1. Parent of i is at i//2; children of i are at 2i (left) and 2i+1 (right). No pointer storage needed.',
        },
        {
          title: 'O(1) Peek, O(log n) Insert/Extract',
          desc: 'The min (or max) is always at the root — O(1) access. Insertions sift up and extractions sift down in O(log n) = O(height).',
        },
        {
          title: 'Build Heap in O(n)',
          desc: 'Calling heapify on each non-leaf node from bottom to top builds a valid heap from an arbitrary array in O(n) — faster than n individual insertions which would take O(n log n).',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Array Storage Layout',
        desc: 'Represent the complete binary tree as an array. Index 0 (or 1) is the root. For node at index i, left child = 2i+1, right child = 2i+2 (0-based).',
      },
      {
        n: '2',
        label: 'Insert — Append and Sift Up',
        desc: 'Append the new element to the end of the array, then repeatedly swap it with its parent while it violates the heap property (sift up / heapify up).',
      },
      {
        n: '3',
        label: 'Peek Min/Max',
        desc: 'The root element at index 0 (or 1) is always the minimum (min-heap) or maximum (max-heap). Read in O(1) without modifying the heap.',
      },
      {
        n: '4',
        label: 'Extract Min/Max — Swap Root and Sift Down',
        desc: 'Swap the root with the last element, remove the last element, then sift the new root down by swapping with the smaller (or larger) child until heap property is restored.',
      },
      {
        n: '5',
        label: 'Build Heap — Heapify from Bottom',
        desc: 'Start from the last non-leaf node (index n//2 - 1) and call sift-down on every node up to the root. Runs in O(n) total.',
      },
    ],
    codeExamples: [
      {
        label: 'Min-Heap — insert and extract_min',
        code: [
          'class MinHeap:',
          '    def __init__(self):',
          '        self.heap = []',
          '',
          '    def push(self, val):',
          '        self.heap.append(val)',
          '        self._sift_up(len(self.heap) - 1)',
          '',
          '    def pop(self):',
          '        if len(self.heap) == 1:',
          '            return self.heap.pop()',
          '        root = self.heap[0]',
          '        self.heap[0] = self.heap.pop()   # move last to root',
          '        self._sift_down(0)',
          '        return root',
          '',
          '    def _sift_up(self, i):',
          '        while i > 0:',
          '            parent = (i - 1) // 2',
          '            if self.heap[i] < self.heap[parent]:',
          '                self.heap[i], self.heap[parent] = self.heap[parent], self.heap[i]',
          '                i = parent',
          '            else: break',
          '',
          '    def _sift_down(self, i):',
          '        n = len(self.heap)',
          '        while True:',
          '            smallest, l, r = i, 2*i+1, 2*i+2',
          '            if l < n and self.heap[l] < self.heap[smallest]: smallest = l',
          '            if r < n and self.heap[r] < self.heap[smallest]: smallest = r',
          '            if smallest == i: break',
          '            self.heap[i], self.heap[smallest] = self.heap[smallest], self.heap[i]',
          '            i = smallest',
        ].join('\n'),
        complexity: 'Push O(log n) | Pop O(log n) | Peek O(1)',
        variant: 'default',
      },
      {
        label: 'Build heap in O(n)',
        code: [
          'def build_heap(arr):',
          '    n = len(arr)',
          '    # Start from last non-leaf and sift down each node',
          '    for i in range(n // 2 - 1, -1, -1):',
          '        sift_down(arr, n, i)',
          '    return arr',
          '',
          'def sift_down(arr, n, i):',
          '    while True:',
          '        smallest, l, r = i, 2*i+1, 2*i+2',
          '        if l < n and arr[l] < arr[smallest]: smallest = l',
          '        if r < n and arr[r] < arr[smallest]: smallest = r',
          '        if smallest == i: break',
          '        arr[i], arr[smallest] = arr[smallest], arr[i]',
          '        i = smallest',
          '',
          'data = [9, 4, 7, 1, 8, 3]',
          'build_heap(data)',
          'print(data[0])  # 1 (minimum at root)',
        ].join('\n'),
        complexity: 'Build O(n) | each sift-down O(log n) but amortised O(n) total',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Insert (push)', value: 'O(log n)', color: 'text-cyan-400', note: 'Sift up at most h = log n levels' },
        { case: 'Extract Min/Max (pop)', value: 'O(log n)', color: 'text-cyan-400', note: 'Sift down at most h = log n levels' },
        { case: 'Peek Min/Max', value: 'O(1)', color: 'text-emerald-400', note: 'Root is always min (or max) — direct access' },
        { case: 'Build Heap', value: 'O(n)', color: 'text-indigo-400', note: 'Bottom-up heapify; lower nodes do less work' },
      ],
      note: 'Building a heap from n items takes O(n) using bottom-up heapify — counterintuitively faster than n * O(log n) individual inserts.',
    },
    realWorld: [
      { title: 'Priority Queues', desc: 'OS task schedulers, event simulation systems, and bandwidth managers use min/max heaps to always process the highest-priority item next.' },
      { title: "Dijkstra's Algorithm", desc: "Shortest-path algorithms use a min-heap to greedily extract the next closest unvisited vertex in O(log V) per step." },
      { title: 'Heap Sort', desc: 'Build a max-heap from the array then repeatedly extract the maximum. Gives O(n log n) in-place sort with O(1) extra space.' },
      { title: 'Median Finding with Two Heaps', desc: 'Maintain a max-heap of the lower half and a min-heap of the upper half to find the running median of a data stream in O(log n) per element.' },
    ],
    videoId: "3cR8V8hzwQ4",
    videoTitle: "Heap Data Structure Explained: Min-Heap & Max-Heap Operations",
    quiz: [
      {
        q: 'What is the heap property in a min-heap?',
        options: [
          'Every child is greater than or equal to its parent',
          'Every parent is less than or equal to its children',
          'Every node is equal to its sibling',
          'The tree must be a complete binary search tree',
        ],
        correct: 1,
        explanation: 'In a min-heap, every parent node is less than or equal to both its children. This ensures the minimum element is always at the root.',
      },
      {
        q: 'What is the time complexity of extracting the maximum from a max-heap?',
        options: ['O(1)', 'O(log n)', 'O(n)', 'O(n log n)'],
        correct: 1,
        explanation: 'Extracting the max requires swapping the root with the last element, removing the last, and sifting the new root down. Sift-down is O(h) = O(log n) for a complete binary tree.',
      },
      {
        q: 'Why is building a heap from n elements O(n) rather than O(n log n)?',
        options: [
          'Because we skip half the elements',
          'Because rotations are O(1) in heaps',
          'Because nodes near the bottom have smaller subtrees and do less work during sift-down; the total work sums to O(n)',
          'Because heaps use a hash table internally',
        ],
        correct: 2,
        explanation: 'Bottom-up heapify starts from the last non-leaf. Leaf nodes do O(1) work, nodes one level up do O(1) work, etc. The geometric series sums to O(n). In contrast, n sequential inserts each take O(log n) for O(n log n) total.',
      },
      {
        q: 'In a zero-indexed array representing a heap, what are the indices of the children of the node at index i?',
        options: [
          'i - 1 and i + 1',
          'i * 2 and i * 2 + 1',
          '2 * i + 1 and 2 * i + 2',
          'i / 2 and i / 2 + 1',
        ],
        correct: 2,
        explanation: 'With 0-based indexing, the left child of node i is at 2*i + 1 and the right child is at 2*i + 2. The parent of node i is at (i - 1) // 2. This arithmetic replaces the need for explicit pointers.',
      },
      {
        q: 'Which problem can be solved efficiently using two heaps — a max-heap and a min-heap?',
        options: [
          'Sorting an array in O(n log n)',
          'Finding the running median of a data stream',
          'Computing the shortest path in a graph',
          'Checking if an array is sorted',
        ],
        correct: 1,
        explanation: 'To find the running median, maintain a max-heap of the lower half and a min-heap of the upper half. After each insertion, balance the heaps so their sizes differ by at most 1. The median is the root of one (or the average of both roots).',
      },
      {
        q: 'What is the key structural property that distinguishes a heap from a general binary tree?',
        options: [
          'A heap is always a complete binary tree stored in level order',
          'A heap always has an odd number of nodes',
          'A heap requires all leaf nodes to be at the same depth',
          'A heap nodes must store only integers',
        ],
        correct: 0,
        explanation: 'A heap is a complete binary tree — every level is fully filled except possibly the last, which is filled from left to right. This completeness guarantees height O(log n) and enables the compact array representation without pointers.',
      },
    ],
  },

  'tries': {
    concept: {
      overview:
        'A trie (also called a prefix tree or digital tree) is a tree where each node represents a single character and paths from the root to marked end-of-word nodes spell out complete strings. Every operation (insert, search, delete) runs in O(L) time where L is the length of the string — independent of the number of stored strings. Tries excel at prefix-based queries that hash maps cannot handle efficiently.',
      keyPoints: [
        {
          title: 'O(L) Operations Regardless of Dictionary Size',
          desc: 'Searching, inserting, or deleting a string of length L takes exactly O(L) time whether the trie stores 100 or 1 million strings.',
        },
        {
          title: 'Perfect for Prefix Queries',
          desc: 'Finding all strings that start with a given prefix is O(L + output size) — simply traverse to the prefix node then collect all descendants.',
        },
        {
          title: 'Higher Memory Usage',
          desc: 'Each node may store up to ALPHABET_SIZE (26 for lowercase English) child pointers. This can use significantly more memory than a hash map for sparse character sets.',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Root Represents the Empty String',
        desc: 'The root node does not contain a character. All strings share the root as their common ancestor.',
      },
      {
        n: '2',
        label: 'Each Edge is a Character',
        desc: 'Moving from a parent to a child via edge labelled "c" appends character "c" to the current prefix.',
      },
      {
        n: '3',
        label: 'Insert — Follow or Create Nodes',
        desc: 'For each character in the word, follow the existing child edge or create a new node. After the last character, mark that node as end_of_word = True.',
      },
      {
        n: '4',
        label: 'Search — Follow Edges Character by Character',
        desc: 'Traverse the trie one character at a time. If a required child is missing, return False. If all characters are consumed, return the node\'s end_of_word flag.',
      },
      {
        n: '5',
        label: 'Prefix Search — Stop at Prefix Node',
        desc: 'To find all words with a given prefix, traverse to the end of the prefix then collect all paths leading to end_of_word nodes below that point.',
      },
    ],
    codeExamples: [
      {
        label: 'Trie — TrieNode class, insert, search',
        code: [
          'class TrieNode:',
          '    def __init__(self):',
          '        self.children   = {}   # char -> TrieNode',
          '        self.is_end     = False',
          '',
          'class Trie:',
          '    def __init__(self):',
          '        self.root = TrieNode()',
          '',
          '    def insert(self, word):',
          '        node = self.root',
          '        for ch in word:',
          '            if ch not in node.children:',
          '                node.children[ch] = TrieNode()',
          '            node = node.children[ch]',
          '        node.is_end = True',
          '',
          '    def search(self, word):',
          '        node = self.root',
          '        for ch in word:',
          '            if ch not in node.children:',
          '                return False',
          '            node = node.children[ch]',
          '        return node.is_end',
          '',
          '    def starts_with(self, prefix):',
          '        node = self.root',
          '        for ch in prefix:',
          '            if ch not in node.children:',
          '                return False',
          '            node = node.children[ch]',
          '        return True',
          '',
          't = Trie()',
          'for w in ["apple", "app", "apt", "bat"]:',
          '    t.insert(w)',
          'print(t.search("app"))        # True',
          'print(t.search("ap"))         # False (not marked as end)',
          'print(t.starts_with("ap"))    # True',
        ].join('\n'),
        complexity: 'Insert/Search O(L) | Space O(ALPHABET * L * N)',
        variant: 'default',
      },
      {
        label: 'Auto-complete — collect all words with a prefix',
        code: [
          'def autocomplete(trie, prefix):',
          '    node = trie.root',
          '    for ch in prefix:',
          '        if ch not in node.children:',
          '            return []',
          '        node = node.children[ch]',
          '    results = []',
          '    _dfs(node, prefix, results)',
          '    return results',
          '',
          'def _dfs(node, current, results):',
          '    if node.is_end:',
          '        results.append(current)',
          '    for ch, child in node.children.items():',
          '        _dfs(child, current + ch, results)',
          '',
          'print(autocomplete(t, "ap"))  # ["app", "apple", "apt"]',
        ].join('\n'),
        complexity: 'O(L + output_size)',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Insert', value: 'O(L)', color: 'text-indigo-400', note: 'One node per character of the word' },
        { case: 'Search / Prefix Check', value: 'O(L)', color: 'text-indigo-400', note: 'Traverse one edge per character' },
        { case: 'Delete', value: 'O(L)', color: 'text-indigo-400', note: 'Mark end_of_word false; optionally prune unused nodes' },
        { case: 'Space', value: 'O(A * L * N)', color: 'text-amber-400', note: 'A = alphabet size, L = avg length, N = number of words' },
      ],
      note: 'Compressed tries (radix trees / Patricia trees) merge single-child chains to reduce space while preserving O(L) operations.',
    },
    realWorld: [
      { title: 'Autocomplete and Autocorrect', desc: 'Search engines and keyboard apps use tries to instantly find all completions of a typed prefix or suggest corrections for misspellings.' },
      { title: 'Spell Checker', desc: 'Word processors traverse a trie to check whether a typed word exists and suggest the nearest valid prefixes or suffixes.' },
      { title: 'IP Routing Tables', desc: 'Network routers use binary tries (Patricia trees) to perform longest-prefix matching on IP addresses in O(32) = O(1) steps.' },
      { title: 'Word Games', desc: 'Games like Boggle and Scrabble use tries for rapid valid-word lookup during board traversal, pruning paths whose prefixes have no valid completions.' },
    ],
    videoId: "3CbFFVHQrk4",
    videoTitle: "The Trie Data Structure (Prefix Tree)",
    quiz: [
      {
        q: 'What is the time complexity of searching for a word of length L in a trie?',
        options: ['O(n) where n = number of words stored', 'O(L * n)', 'O(L)', 'O(log n)'],
        correct: 2,
        explanation: 'A trie search follows exactly L edges — one per character in the word — regardless of how many strings are stored in the trie.',
      },
      {
        q: 'What is the main advantage of a trie over a hash map for storing strings?',
        options: [
          'Tries use less memory than hash maps',
          'Tries support O(L) prefix queries, which hash maps cannot efficiently support',
          'Tries have O(1) average lookup just like hash maps',
          'Tries never have hash collisions',
        ],
        correct: 1,
        explanation: 'The key trie advantage is efficient prefix operations: finding all words sharing a prefix, longest common prefix, and lexicographic ordering. Hash maps hash entire strings and cannot exploit shared prefixes.',
      },
      {
        q: 'What is a compressed trie (radix tree)?',
        options: [
          'A trie where each node stores an entire word',
          'A trie where chains of single-child nodes are merged into one edge labelled with a substring',
          'A trie stored as a sorted array instead of a tree',
          'A trie with a hash map at each level',
        ],
        correct: 1,
        explanation: 'A compressed trie (radix tree / Patricia tree) collapses chains of nodes that each have only one child into a single edge labelled with the combined substring, reducing space while keeping O(L) operations.',
      },
      {
        q: 'What is the space complexity of a trie that stores N words of average length L over an alphabet of size A?',
        options: ['O(N)', 'O(L)', 'O(A × L × N)', 'O(N × L²)'],
        correct: 2,
        explanation: 'In the worst case (no shared prefixes) each of the N words creates L nodes, and each node allocates space for up to A child pointers, giving O(A × L × N) total space. In practice sharing of common prefixes reduces this significantly.',
      },
      {
        q: 'How does a trie handle the distinction between the word "app" and the prefix "app" in "apple"?',
        options: [
          'It stores duplicate nodes for each word',
          'Each node has an is_end flag; "app" sets is_end=True at its final node while "apple" continues to additional nodes',
          'It uses different edge labels for full words vs prefixes',
          'It is impossible; "app" and "apple" cannot coexist in a trie',
        ],
        correct: 1,
        explanation: 'Tries mark the end of complete words with an is_end (or end_of_word) boolean flag on each node. Both "app" and "apple" share the path a→p→p; the "p" node has is_end=True for "app", and the tree continues with "l"→"e" for "apple".',
      },
      {
        q: 'Why are tries preferred over hash maps for IP routing table lookups?',
        options: [
          'Tries have O(1) lookup while hash maps are O(n)',
          'Tries naturally support longest-prefix matching, which is required for routing decisions',
          'Tries use less memory than hash maps for IP addresses',
          'Hash maps cannot store IP addresses',
        ],
        correct: 1,
        explanation: 'IP routing requires longest-prefix matching — finding the most specific route that matches a destination address. Tries enable this in O(32) steps for IPv4 by traversing bit by bit. Hash maps match exact keys and cannot efficiently find the longest matching prefix.',
      },
    ],
  },

  'segment-trees': {
    concept: {
      overview:
        'A segment tree is a full binary tree built over an array that stores aggregated results (sum, min, max, GCD, etc.) for contiguous sub-ranges. Each leaf holds one array element; each internal node holds the aggregate of its children\'s range. Once built in O(n), range queries and point updates each take O(log n), making it powerful for repeated interval operations.',
      keyPoints: [
        {
          title: 'Each Node Stores a Range Aggregate',
          desc: 'A node covering [l, r] stores aggregate(arr[l..r]). Internal nodes combine their two children: node = combine(left_child, right_child).',
        },
        {
          title: 'Build O(n), Query and Update O(log n)',
          desc: 'Building fills all 4n nodes bottom-up in O(n). A range query decomposes the query range into O(log n) disjoint stored segments. A point update changes one leaf and propagates up in O(log n).',
        },
        {
          title: 'Lazy Propagation for Range Updates',
          desc: 'When a range update applies to many nodes, lazy propagation defers updates to children until they are actually queried, keeping range updates O(log n) as well.',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Build — Leaf Nodes',
        desc: 'For each array index i, store arr[i] in the corresponding leaf. Leaf for index i is typically at tree[n + i] in a 1-indexed segment tree.',
      },
      {
        n: '2',
        label: 'Build — Internal Nodes (bottom-up)',
        desc: 'For each internal node at position p covering [l, r], compute tree[p] = combine(tree[2p], tree[2p+1]) where the midpoint is m = (l + r) / 2.',
      },
      {
        n: '3',
        label: 'Range Query',
        desc: 'Decompose the query [l, r] recursively: if the node\'s range is fully inside [l, r] return its value; if fully outside return identity; otherwise combine both children\'s answers.',
      },
      {
        n: '4',
        label: 'Point Update',
        desc: 'Update the target leaf to the new value, then walk up the tree updating each ancestor: tree[p] = combine(tree[2p], tree[2p+1]).',
      },
      {
        n: '5',
        label: 'Choose the Aggregate Function',
        desc: 'Common aggregates: sum (identity=0), minimum (identity=+INF), maximum (identity=-INF), GCD (identity=0). The segment tree structure is the same for all.',
      },
    ],
    codeExamples: [
      {
        label: 'Segment Tree — range sum query',
        code: [
          'class SegTree:',
          '    def __init__(self, arr):',
          '        self.n    = len(arr)',
          '        self.tree = [0] * (4 * self.n)',
          '        self._build(arr, 0, 0, self.n - 1)',
          '',
          '    def _build(self, arr, node, start, end):',
          '        if start == end:',
          '            self.tree[node] = arr[start]',
          '        else:',
          '            mid = (start + end) // 2',
          '            self._build(arr, 2*node+1, start,   mid)',
          '            self._build(arr, 2*node+2, mid+1,   end)',
          '            self.tree[node] = self.tree[2*node+1] + self.tree[2*node+2]',
          '',
          '    def query(self, l, r, node=0, start=None, end=None):',
          '        if start is None: start, end = 0, self.n - 1',
          '        if r < start or end < l: return 0          # outside',
          '        if l <= start and end <= r: return self.tree[node]  # fully inside',
          '        mid = (start + end) // 2',
          '        left  = self.query(l, r, 2*node+1, start,   mid)',
          '        right = self.query(l, r, 2*node+2, mid+1,   end)',
          '        return left + right',
          '',
          'st = SegTree([1, 3, 5, 7, 9, 11])',
          'print(st.query(1, 3))   # 3+5+7 = 15',
          'print(st.query(0, 5))   # 1+3+5+7+9+11 = 36',
        ].join('\n'),
        complexity: 'Build O(n) | Query O(log n) | Update O(log n)',
        variant: 'default',
      },
      {
        label: 'Point update',
        code: [
          '    def update(self, idx, val, node=0, start=None, end=None):',
          '        if start is None: start, end = 0, self.n - 1',
          '        if start == end:',
          '            self.tree[node] = val',
          '        else:',
          '            mid = (start + end) // 2',
          '            if idx <= mid:',
          '                self.update(idx, val, 2*node+1, start, mid)',
          '            else:',
          '                self.update(idx, val, 2*node+2, mid+1, end)',
          '            self.tree[node] = self.tree[2*node+1] + self.tree[2*node+2]',
          '',
          '# Update arr[2] from 5 to 10',
          'st.update(2, 10)',
          'print(st.query(1, 3))   # 3+10+7 = 20',
        ].join('\n'),
        complexity: 'Update O(log n) — traverses one root-to-leaf path',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Build', value: 'O(n)', color: 'text-indigo-400', note: 'Fill all 4n nodes once bottom-up' },
        { case: 'Range Query', value: 'O(log n)', color: 'text-cyan-400', note: 'At most O(log n) disjoint segments visited' },
        { case: 'Point Update', value: 'O(log n)', color: 'text-cyan-400', note: 'Update leaf + propagate up log n ancestors' },
        { case: 'Space', value: 'O(n)', color: 'text-indigo-400', note: 'Tree array of size 4n' },
      ],
      note: 'With lazy propagation, range updates also become O(log n). Without it, a range update touching all n leaves is O(n).',
    },
    realWorld: [
      { title: 'Range Minimum Query (RMQ)', desc: 'Given an array, answer "what is the minimum in arr[l..r]?" repeatedly. Segment trees answer each query in O(log n) after O(n) build.' },
      { title: 'Range Sum Queries', desc: 'Database-style aggregate queries over changing data (e.g., sum of sales in a date range with daily updates) use segment trees.' },
      { title: 'Computational Geometry', desc: 'Rectangle union area, interval stabbing, and sweepline algorithms use segment trees as a core data structure.' },
      { title: 'Competitive Programming', desc: 'Segment trees appear in roughly 20% of competitive programming problems that involve intervals, making them one of the most important structures to master.' },
    ],
    videoId: "Ic7OO3Uw6J0",
    videoTitle: "Segment Trees - The Best Introduction in 10 Minutes",
    quiz: [
      {
        q: 'What is the time complexity of a range query on a segment tree?',
        options: ['O(n)', 'O(1)', 'O(log n)', 'O(n log n)'],
        correct: 2,
        explanation: 'A range query decomposes the target interval into at most O(log n) disjoint stored segments and combines their pre-computed values, giving O(log n) per query.',
      },
      {
        q: 'How much space does a segment tree use for an array of n elements?',
        options: ['O(log n)', 'O(n)', 'O(n log n)', 'O(n²)'],
        correct: 1,
        explanation: 'A segment tree uses at most 4n nodes (the tree array is allocated as 4 * n). This is O(n) space.',
      },
      {
        q: 'What is lazy propagation in segment trees used for?',
        options: [
          'To speed up the build phase to O(log n)',
          'To handle range updates efficiently in O(log n) by deferring child updates until they are queried',
          'To reduce space from O(n) to O(log n)',
          'To make point queries O(1)',
        ],
        correct: 1,
        explanation: 'Without lazy propagation, updating a range of n elements requires touching all O(n) leaves. Lazy propagation tags nodes with pending updates and defers propagating them to children until a query forces it, keeping range updates O(log n).',
      },
      {
        q: 'Why is a segment tree array typically allocated with size 4n rather than 2n?',
        options: [
          'To support lazy propagation nodes',
          'Because the tree may not be perfectly balanced, and padding to the next power of 2 can require up to 4n slots',
          'To store both the original array and the tree simultaneously',
          'To handle dynamic insertions without reallocation',
        ],
        correct: 1,
        explanation: 'A segment tree for n elements has at most 2 × (next power of 2 ≥ n) - 1 nodes. In the worst case this approaches 4n. Allocating 4n slots is a safe upper bound that avoids out-of-bounds access regardless of n.',
      },
      {
        q: 'Which aggregate functions can a segment tree support natively without modification?',
        options: [
          'Only sum',
          'Only min and max',
          'Any associative function such as sum, min, max, GCD, XOR',
          'Any function, including non-associative ones like average',
        ],
        correct: 2,
        explanation: 'Segment trees work with any associative "combine" function — sum, min, max, GCD, XOR, product, etc. The structure only requires that you can combine two child results to get the parent result. Non-associative functions (like average directly) require additional bookkeeping.',
      },
      {
        q: 'What happens to a segment tree range query when the queried interval [l, r] is completely outside the current node\'s interval?',
        options: [
          'The function throws an error',
          'It returns the identity element for the aggregate (e.g., 0 for sum, +∞ for min)',
          'It returns the node\'s stored value unchanged',
          'It recurses into both children anyway',
        ],
        correct: 1,
        explanation: 'When the query range is completely disjoint from the node\'s range, the recursive function immediately returns the identity element (0 for sum, +∞ for min, -∞ for max). This allows the result to be safely combined with other subtree results without affecting the answer.',
      },
    ],
  },

  'fenwick-trees': {
    concept: {
      overview:
        'A Fenwick tree (Binary Indexed Tree, or BIT) is a compact data structure that supports prefix-sum queries and point updates in O(log n) using a clever bit-manipulation trick. It is stored as a flat array where each index i is responsible for a range of elements determined by the lowest set bit (LSB) of i. Compared to segment trees, BITs are simpler to code, use less memory, and have smaller constant factors.',
      keyPoints: [
        {
          title: 'Lowest Set Bit (LSB) Trick',
          desc: 'The key operation is i & (-i) which extracts the lowest set bit of i. For update, add LSB to move to the next responsible ancestor. For query, subtract LSB to move to the next partial-sum range.',
        },
        {
          title: 'Prefix Sums and Range Queries',
          desc: 'prefix(i) returns sum of arr[1..i]. Range query [l, r] = prefix(r) - prefix(l-1). Supports any commutative invertible operation (sum, XOR) but not min/max directly.',
        },
        {
          title: 'More Compact Than Segment Trees',
          desc: 'BIT uses a single array of size n+1 (O(n)) with no 4x overhead, and its iterative implementation fits in ~5 lines of code.',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Initialize — 1-Indexed Array',
        desc: 'Create a BIT array of size n+1 filled with zeros. Use 1-based indexing to make the LSB trick work cleanly.',
      },
      {
        n: '2',
        label: 'Update — Add Value and Propagate',
        desc: 'To add delta to position i: bit[i] += delta, then i += i & (-i) (add LSB). Repeat until i > n. This updates all ranges responsible for position i.',
      },
      {
        n: '3',
        label: 'Prefix Query — Sum from 1 to i',
        desc: 'To compute prefix sum up to i: total += bit[i], then i -= i & (-i) (remove LSB). Repeat until i == 0. This collects all partial sums that cover index i.',
      },
      {
        n: '4',
        label: 'Range Query [l, r]',
        desc: 'Range sum from l to r = prefix(r) - prefix(l - 1). Requires two prefix calls — total O(log n).',
      },
      {
        n: '5',
        label: 'Build from Array',
        desc: 'Call update(i, arr[i]) for each element. This takes O(n log n). Alternatively, an O(n) build is possible by direct summation.',
      },
    ],
    codeExamples: [
      {
        label: 'Binary Indexed Tree — update and prefix query',
        code: [
          'class BIT:',
          '    def __init__(self, n):',
          '        self.n   = n',
          '        self.bit = [0] * (n + 1)  # 1-indexed',
          '',
          '    def update(self, i, delta):',
          '        """Add delta to position i (1-indexed)."""',
          '        while i <= self.n:',
          '            self.bit[i] += delta',
          '            i += i & (-i)    # move to next responsible ancestor',
          '',
          '    def prefix(self, i):',
          '        """Sum of arr[1..i]."""',
          '        total = 0',
          '        while i > 0:',
          '            total += self.bit[i]',
          '            i -= i & (-i)    # remove LSB, move to next partial range',
          '        return total',
          '',
          '    def range_query(self, l, r):',
          '        return self.prefix(r) - self.prefix(l - 1)',
          '',
          'bit = BIT(6)',
          'for i, v in enumerate([1, 3, 5, 7, 9, 11], start=1):',
          '    bit.update(i, v)',
          'print(bit.range_query(2, 4))  # 3+5+7 = 15',
          'print(bit.prefix(6))          # 1+3+5+7+9+11 = 36',
        ].join('\n'),
        complexity: 'Update O(log n) | Prefix Query O(log n) | Space O(n)',
        variant: 'default',
      },
      {
        label: 'LSB trick explained and point update',
        code: [
          '# i & (-i) isolates the lowest set bit of i',
          '# Examples:',
          '#   i=6  (110 binary) -> i&(-i) = 010 = 2',
          '#   i=4  (100 binary) -> i&(-i) = 100 = 4',
          '#   i=3  (011 binary) -> i&(-i) = 001 = 1',
          '',
          '# Responsibility ranges:',
          '#   bit[1] covers [1,1]  (LSB=1, covers 1 element)',
          '#   bit[2] covers [1,2]  (LSB=2, covers 2 elements)',
          '#   bit[4] covers [1,4]  (LSB=4, covers 4 elements)',
          '#   bit[6] covers [5,6]  (LSB=2, covers 2 elements)',
          '',
          '# Point update at index 3: add 10',
          'bit.update(3, 10)',
          'print(bit.range_query(1, 3))  # 1+3+(5+10) = 19',
        ].join('\n'),
        complexity: 'Each update/query step is O(1); at most log₂ n steps',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Point Update', value: 'O(log n)', color: 'text-cyan-400', note: 'At most log n ancestors to update' },
        { case: 'Prefix Query', value: 'O(log n)', color: 'text-cyan-400', note: 'At most log n partial-sum ranges to collect' },
        { case: 'Build (n updates)', value: 'O(n log n)', color: 'text-amber-400', note: 'n calls to update(); O(n) build also possible' },
        { case: 'Space', value: 'O(n)', color: 'text-indigo-400', note: 'Single array of size n+1, no extra overhead' },
      ],
      note: 'BITs only support invertible operations (sum, XOR) for range queries. For min/max range queries use a segment tree instead.',
    },
    realWorld: [
      { title: 'Order Statistics', desc: 'Count elements less than a value by compressing values to indices and running prefix queries on a BIT updated with frequency counts.' },
      { title: 'Counting Inversions', desc: 'Sort an array and count how many pairs (i, j) with i < j have arr[i] > arr[j] — a classic BIT application solved in O(n log n).' },
      { title: 'Range Sum Queries on Changing Data', desc: 'Financial systems tracking cumulative totals with frequent point updates use BITs for their simplicity and cache efficiency.' },
      { title: 'Competitive Programming Favourite', desc: 'BITs appear frequently in contest problems involving frequency tables, rank queries, and dynamic prefix sums due to their minimal code footprint.' },
    ],
    videoId: "IvBrbRfZgD4",
    videoTitle: "Master the Fenwick Tree (Binary Indexed Tree) - Efficient Prefix Sums",
    quiz: [
      {
        q: 'What does BIT stand for in the context of Fenwick trees?',
        options: [
          'Balanced Interval Tree',
          'Binary Indexed Tree',
          'Breadth-first Indexed Table',
          'Bitwise Interval Tracker',
        ],
        correct: 1,
        explanation: 'BIT stands for Binary Indexed Tree, named after Peter Fenwick\'s 1994 paper. The "binary indexed" refers to using binary representation of indices to determine responsibility ranges.',
      },
      {
        q: 'What is the key bit-manipulation trick used in a Fenwick tree?',
        options: [
          'i XOR (i - 1) to toggle the lowest bit',
          'i & (-i) to extract the lowest set bit (LSB)',
          'i >> 1 to halve the index at each step',
          'i | (i + 1) to set the next higher bit',
        ],
        correct: 1,
        explanation: 'i & (-i) extracts the lowest set bit of i. Adding it during update moves to the next ancestor responsible range. Subtracting it during query moves to the next partial-sum range to collect.',
      },
      {
        q: 'How do you compute a range sum query [l, r] using a Fenwick tree?',
        options: [
          'Directly query the BIT with range parameters l and r',
          'prefix(r) - prefix(l - 1), using two prefix calls',
          'Build a separate range-query array',
          'Only prefix queries are supported; range queries require a segment tree',
        ],
        correct: 1,
        explanation: 'Because BITs support prefix sums efficiently, a range sum [l, r] = prefix(r) - prefix(l - 1). This requires exactly two O(log n) prefix queries for O(log n) total.',
      },
      {
        q: 'Why does a Fenwick tree use 1-based indexing instead of 0-based indexing?',
        options: [
          'Because 0-based indexing causes off-by-one errors in all loops',
          'Because i & (-i) = 0 when i = 0, making the LSB trick break at index 0',
          'Because arrays in most languages start at 1',
          'Because prefix sums naturally start at index 1',
        ],
        correct: 1,
        explanation: 'The core operation i & (-i) isolates the lowest set bit. When i = 0, both 0 & (-0) = 0 and the loop i -= i & (-i) would loop forever (or never advance). Using 1-based indexing ensures i is always positive and the LSB trick terminates correctly.',
      },
      {
        q: 'Which type of range query cannot be directly supported by a standard Fenwick tree?',
        options: [
          'Range sum query',
          'Range XOR query',
          'Range minimum query',
          'Prefix frequency count',
        ],
        correct: 2,
        explanation: 'Fenwick trees only support invertible (group) operations where range(l, r) = prefix(r) - prefix(l-1) holds. Minimum is not invertible — knowing min(1..r) and min(1..l-1) does not let you derive min(l..r). Range minimum queries require a segment tree or sparse table.',
      },
      {
        q: 'What is the time complexity of building a Fenwick tree by calling update() for each of the n elements?',
        options: ['O(n)', 'O(n log n)', 'O(n²)', 'O(log n)'],
        correct: 1,
        explanation: 'Each update() call traverses at most log n ancestors (the LSB-ascending chain). Calling update() for all n elements therefore takes O(n log n) total. An O(n) build is possible by direct prefix-sum construction, but the update-based approach is more commonly used for simplicity.',
      },
    ],
  },

  'binary-search': {
    concept: {
      overview:
        'Binary Search finds a target in a SORTED array by repeatedly halving the search space. Compare target to the middle element: if equal you\'re done, if smaller search left half, if larger search right half. O(log n) — 1 million elements needs at most 20 comparisons.',
      keyPoints: [
        {
          title: 'Requires Sorted',
          desc: 'Binary search only works on sorted arrays. Applying it to unsorted data produces incorrect results.',
        },
        {
          title: 'O(log n)',
          desc: 'Each comparison halves the remaining search space, giving O(log n) time — 1 million elements needs at most 20 comparisons.',
        },
        {
          title: 'Mid Formula',
          desc: 'Use mid = low + (high - low) / 2 to avoid integer overflow instead of (low + high) / 2.',
        },
      ],
    },
    steps: [
      {
        n: '1',
        label: 'Set Bounds',
        desc: 'Initialise low = 0 and high = n - 1, covering the full array.',
      },
      {
        n: '2',
        label: 'Find Mid',
        desc: 'Compute mid = low + (high - low) // 2 to get the middle index without overflow.',
      },
      {
        n: '3',
        label: 'Compare',
        desc: 'If arr[mid] == target: element found — return mid.',
      },
      {
        n: '4',
        label: 'Halve',
        desc: 'If target < arr[mid]: set high = mid - 1 (search left half). Otherwise set low = mid + 1 (search right half).',
      },
      {
        n: '5',
        label: 'Repeat or Fail',
        desc: 'Continue the loop while low <= high. If the loop exits without a match, return -1 to signal the target is absent.',
      },
    ],
    codeExamples: [
      {
        label: 'Binary Search — iterative',
        code: [
          'def binary_search(arr, target):',
          '    low, high = 0, len(arr) - 1',
          '    while low <= high:',
          '        mid = low + (high - low) // 2',
          '        if arr[mid] == target:',
          '            return mid',
          '        elif target < arr[mid]:',
          '            high = mid - 1',
          '        else:',
          '            low = mid + 1',
          '    return -1',
          '',
          '# Example',
          'nums = [1, 3, 5, 7, 9, 11, 13]',
          'print(binary_search(nums, 7))   # 3',
          'print(binary_search(nums, 6))   # -1',
        ].join('\n'),
        complexity: 'Time O(log n) | Space O(1)',
        variant: 'default',
      },
      {
        label: 'Binary Search — recursive',
        code: [
          'def binary_search_recursive(arr, target, low=0, high=None):',
          '    if high is None:',
          '        high = len(arr) - 1',
          '    if low > high:',
          '        return -1',
          '    mid = low + (high - low) // 2',
          '    if arr[mid] == target:',
          '        return mid',
          '    elif target < arr[mid]:',
          '        return binary_search_recursive(arr, target, low, mid - 1)',
          '    else:',
          '        return binary_search_recursive(arr, target, mid + 1, high)',
          '',
          '# Note: recursive version uses O(log n) call-stack space',
          'nums = [2, 4, 6, 8, 10]',
          'print(binary_search_recursive(nums, 6))  # 2',
          'print(binary_search_recursive(nums, 5))  # -1',
        ].join('\n'),
        complexity: 'Time O(log n) | Space O(log n) recursive call stack',
        variant: 'good',
      },
    ],
    complexity: {
      rows: [
        { case: 'Best Case', value: 'O(1)', color: 'text-emerald-400', note: 'Target is the middle element on the first check' },
        { case: 'Average Case', value: 'O(log n)', color: 'text-cyan-400', note: 'Search space halves each iteration' },
        { case: 'Worst Case', value: 'O(log n)', color: 'text-cyan-400', note: 'Target is at an edge or absent — log n iterations' },
        { case: 'Space (iterative)', value: 'O(1)', color: 'text-emerald-400', note: 'Only a handful of index variables; no extra allocation' },
      ],
      note: 'The recursive variant uses O(log n) call-stack space. Prefer the iterative version when memory is a concern.',
    },
    realWorld: [
      {
        title: 'Database Index Lookup',
        desc: 'B-trees use binary search at each node level to locate keys, enabling O(log n) row lookups on indexed columns.',
      },
      {
        title: 'std::lower_bound / Python bisect',
        desc: 'C++ STL\'s std::lower_bound and Python\'s bisect module provide binary search on sorted containers, used everywhere from standard library internals to competitive programming.',
      },
    ],
    commonMistakes: [
      {
        wrong: 'Applying binary search to an unsorted array',
        explain: 'Binary search relies on sorted order to decide which half to eliminate. On an unsorted array the comparison result is meaningless and the algorithm may miss the target entirely.',
      },
      {
        wrong: 'Computing mid as (low + high) / 2',
        right: 'mid = low + (high - low) / 2',
        explain: 'When low and high are large integers, low + high can overflow a 32-bit integer. Subtracting first keeps the intermediate value within bounds.',
      },
      {
        wrong: 'Using < instead of <= in the loop condition (while low < high)',
        explain: 'The loop must run while low <= high. Using strict < causes the algorithm to skip the last remaining element when low == high, potentially missing the target.',
      },
    ],
    videoId: "xrMppTpoqdw",
    videoTitle: "Learn Binary Search in 10 Minutes",
    quiz: [
      {
        q: 'What precondition must the input satisfy before applying binary search?',
        options: [
          'The array must contain only distinct integers',
          'The array must be sorted',
          'The array must have an even number of elements',
          'The array must be stored in a linked list',
        ],
        correct: 1,
        explanation: 'Binary search requires a sorted array. It exploits the ordering to discard half the remaining elements with each comparison. On unsorted data the algorithm may produce incorrect results.',
      },
      {
        q: 'How many comparisons does binary search need at most to find a target in an array of 256 elements?',
        options: ['256', '128', '16', '8'],
        correct: 3,
        explanation: 'log₂ 256 = 8. Binary search halves the search space each step, so it needs at most 8 comparisons to locate (or rule out) any element in 256 items.',
      },
      {
        q: 'Which formula correctly computes the middle index without risking integer overflow?',
        options: [
          'mid = (low + high) / 2',
          'mid = low + (high - low) / 2',
          'mid = high - low / 2',
          'mid = (high - low) * 2',
        ],
        correct: 1,
        explanation: 'low + (high - low) / 2 first computes the difference (which is always smaller than each bound alone), then adds it to low. This keeps all intermediate values within the integer range, avoiding overflow that (low + high) / 2 can cause when both bounds are large.',
      },
      {
        q: 'What does binary search return when the target is not found in the array?',
        options: [
          'The index of the closest element',
          'The value 0',
          '-1 to indicate the target is absent',
          'It raises an exception',
        ],
        correct: 2,
        explanation: 'By convention binary search returns -1 when the target is not found. The loop exits when low > high, meaning the entire search space has been exhausted without a match.',
      },
      {
        q: 'What is the space complexity of the iterative binary search implementation?',
        options: ['O(n)', 'O(log n)', 'O(1)', 'O(n log n)'],
        correct: 2,
        explanation: 'The iterative version uses only a fixed number of variables (low, high, mid) regardless of input size, giving O(1) space. The recursive version, by contrast, uses O(log n) call-stack frames.',
      },
      {
        q: 'Which real-world use case is most directly powered by binary search?',
        options: [
          'Finding the shortest path in a road network',
          'Locating a record by primary key in a B-tree database index',
          'Rendering pixels on a screen',
          'Sending packets across a network',
        ],
        correct: 1,
        explanation: 'Database B-tree indexes apply binary search at each node level to narrow down to the target key in O(log n) comparisons. This is the canonical real-world application of binary search in production systems.',
      },
    ],
  },
};
